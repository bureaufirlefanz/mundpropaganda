# Veröffentlichbare Werte — PUBLIC_ landet bewusst im Browser-Bundle.
PUBLIC_SANITY_PROJECT_ID=a6bjftwf
PUBLIC_SANITY_DATASET=production

# Lesetoken für Entwürfe. Ohne Präfix und nur serverseitig lesen —
# niemals aus einer Datei re-exportieren, die eine Seite importieren kann.
# SANITY_READ_TOKEN=
