---
/**
 * Die Route. Daten laden, Renderer aufrufen — mehr steht hier nicht.
 *
 * Der Zusammenbau liegt in `seiten/Startseite.astro`, damit die
 * Vorschau-Route aus Aufgabe 6 exakt dasselbe rendern kann. Zwei
 * Zusammenbauten, die dasselbe zeigen sollen, laufen unbemerkt auseinander.
 */
import Startseite from "../seiten/Startseite.astro";
import { ladeStartseite } from "../lib/startseite";

const daten = await ladeStartseite();
---

<Startseite daten={daten} />
